{
  "name": "senpaijepang",
  "version": "0.1.0",
  "private": true,
  "description": "Verified Migration Infrastructure monorepo (MVP)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "js-yaml": "^4.1.1"
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev:api": "npm run dev -w @senpaijepang/api",
    "dev:web-admin": "npm run dev -w @senpaijepang/web-admin",
    "start:api": "npm run start -w @senpaijepang/api",
    "build:web-admin": "npm run build -w @senpaijepang/web-admin",
    "preview:web-admin": "npm run preview -w @senpaijepang/web-admin",
    "dev:all": "node scripts/dev-all.mjs start",
    "stop:all": "node scripts/dev-all.mjs stop",
    "deploy:staging": "node scripts/deploy-staging.mjs",
    "migrate:api": "npm run migrate -w @senpaijepang/api",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "scan:sast": "node scripts/sast-check.mjs",
    "scan:secrets": "node scripts/secret-scan.mjs",
    "check:openapi": "node scripts/openapi-check.mjs",
    "check:dev-all": "bash scripts/check-dev-all.sh",
    "ci:security": "npm run scan:sast && npm run scan:secrets && npm run check:openapi",
    "smoke:local": "bash scripts/smoke-local.sh",
    "smoke:api": "node --env-file-if-exists=.env scripts/smoke-api-flow.mjs",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run ci:security"
  }
}
