describe('Senpai Jepang - Smoke', () => {
  it('launches app and shows home screen (basic)', async () => {
    await driver.pause(3000);
    // TODO: replace with real selectors once accessibility IDs are set
    // Example: const homeTitle = await $('~home_title');
    // await expect(homeTitle).toBeDisplayed();
  });
});
