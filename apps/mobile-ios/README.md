# SenpaiJepang Mobile iOS (MVP Scaffold)

MVP frontend candidate (SDM) diarahkan ke mobile iOS terlebih dulu.

Current scope:
- Workspace placeholder untuk app mobile iOS.
- Integrasi backend target: `apps/api`.
- Web frontend yang aktif untuk operasional internal hanya `apps/dashboard`.

Recommended next step:
1. Bootstrap app React Native/Expo di folder ini.
2. Implement auth + KYC flow dengan API yang sudah tersedia.
3. Hubungkan role dashboard di web untuk `LPK`, `TSK`, `Kaisha`, `Super Admin`.
